[workspace]
members = ["projects/*"]
default-members = [
    "projects/safe-tensors-export",
]
exclude = [
    "projects/.DS_Store",
    "projects/onnx"
]

[profile.release]
lto = true
panic = "abort"
